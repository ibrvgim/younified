import { colors } from '../constants/colors';

export const shadow = {
  elevation: 4,
  shadowColor: colors.black100,
  shadowOpacity: 0.25,
  shadowOffset: { height: 1, width: 1 },
  shadowRadius: 7,
};
